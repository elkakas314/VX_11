57:EXTRA_PORTS=$(docker ps --format '{{.Names}}\t{{.Ports}}' | grep -E '\->' | grep -v 'vx11-tentaculo-link.*8000' | wc -l)
58:if [[ $EXTRA_PORTS -gt 0 ]]; then
59:  echo "FAIL: Found $EXTRA_PORTS containers with published ports outside vx11-tentaculo-link:8000" | tee -a "$OUT/single_entrypoint_check.txt"

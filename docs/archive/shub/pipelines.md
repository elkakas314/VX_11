# Pipelines
Modo C: RAW → Normalización → FFT → Instrument detection → Track classification → Dynamic range → Tonal profile → Problems → FX suggestion → FX Chain Builder → Preset RPP Generator → JSON VX11 final.

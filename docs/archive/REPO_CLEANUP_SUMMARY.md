# Repo Cleanup Summary

This summary was generated by `scripts/repo_inventory_and_readmes.py`.

## Counts

- ACTIVE: 27295
- CANONICAL_ARCHIVE: 207
- POTENTIAL_ARCHIVE: 84
- TRASH_CANDIDATE: 2

## Output files

- Inventory CSV: docs/archive/repo_inventory.csv
- Summary: docs/archive/REPO_CLEANUP_SUMMARY.md

## Notes

- No files were deleted or moved.
- Classification is conservative: if unsure, files are marked ACTIVE.
- Review `docs/archive/repo_inventory.csv` before making any changes.

## Validation & Commit

- Tests executed: `pytest -k "hermes or switch"` → **128 passed, 3 skipped** (131 collected for this filter). Total runtime ~31.7s.
- Commit: `ff9310d` (created by this operation) — message: `chore: canonical repo cleanup (archive-only, no deletions)`.

## Created files (high level)

- `docs/archive/` (archived audit artifacts and inventory CSV)
- `archive/README.md` (archive top-level README)
- `scripts/repo_inventory_and_readmes.py` (inventory & readme generator)

## Next steps / Reversibility

- To revert: `git revert ff9310d` or inspect `docs/archive/repo_inventory.csv` and selectively restore copies. No ACTIVE files were deleted or moved.
- All actions were archival-only and documented here.

## System Cleanup (Non-Destructive)

- Zombies detected: 0 (see `docs/archive/SYSTEM_PROCESS_CLASSIFICATION.md`).
- Procesos python/uvicorn identificados como `STALE` (candidatos): 12 (no se detuvo ninguno en dry-run).
- Servicios systemd afectados: 0 (solo listado; no se detuvo ni deshabilitó ninguno).
- Servicios VX11 intactos: ✅ (los procesos `uvicorn` pertenecientes a VX11 se identificaron como `CANONICAL` y no se tocaron).
- Reversibilidad garantizada: Todas las acciones fueron dry-run; el script aplicable creado en `scripts/system_cleanup_safe.sh` (por defecto DRY-RUN). Para revertir cualquier cambio aplicado use `git revert <commit>` y/o revisar `logs/system_cleanup_actions.log`.

Archivos relacionados:

- `logs/system_inventory_pre_cleanup.log` — inventario del sistema (solo lectura).
- `docs/archive/SYSTEM_PROCESS_CLASSIFICATION.md` — clasificación heurística de procesos.
- `scripts/system_cleanup_safe.sh` — script seguro (modo dry-run por defecto).
- `logs/system_cleanup_actions.log` — acciones propuestas/ejecutadas (si se ejecuta APPLY=1).


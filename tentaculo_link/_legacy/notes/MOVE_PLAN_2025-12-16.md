# MOVE_PLAN - tentaculo_link (2025-12-16)

Acciones ejecutadas:

1. `tokens.env` (containing live API keys) MOVED -> `tentaculo_link/_legacy/quarantine/DO_NOT_RUN/tokens.env`.
   - Motivo: secrets hardcodeados detectados en repo root.
   - Estado: archivo movido, nueva `tokens.env` redacted creada en repo root con placeholders.

2. Backup de `tentaculo_link` realizado en `.backups/tentaculo_link/backup_20251216_034513`.

Plan futuro (ejecución en próximas fases):

- Revisar archivos legacy (instaladores, zips, scripts) y clasificarlos en `archive/` o `quarantine/` según nivel de riesgo.
- Implementar gateway thin en `tentaculo_link/main.py` + `api/`, `core/`, `db/`, `config/`, `adapters/`.
- Crear tests de humo y reconciliar `docker-compose.yml` si procede.

Reglas aplicadas:
- No borrar archivos; solo mover a `_legacy` con nota y backup.
- Mantener compatibilidad y no ejecutar código peligroso.


# Operator Frontend - Canonical Configuration

# ============================================================
# OPERATOR BACKEND URL (default: http://127.0.0.1:8011)
# ============================================================
VITE_OPERATOR_BASE_URL=http://127.0.0.1:8011

# For Docker: http://operator_backend:8011
# VITE_OPERATOR_BASE_URL=http://operator_backend:8011

# ============================================================
# AUTH - JWT Configuration
# ============================================================
# Default credentials for login screen
# Username: admin (hardcoded in UI)
# Password: comes from user input + backend auth

# Note: JWT tokens are stored in localStorage after login
# All requests to /api/* use Authorization: Bearer <token>

# ============================================================
# LEGACY OPTIONS (for backward compatibility)
# ============================================================
# VITE_VX11_TOKEN=vx11-local-token
# VITE_OPERATOR_SIMPLE_MODE=false

# For WebSocket (if needed for future SSE fallback)
# VITE_WS_BASE_URL=ws://127.0.0.1:8011/ws

VITE_OPERATOR_SIMPLE_MODE=false

# ============================================================
# API Configuration
# ============================================================
# Base URL del backend (por defecto usa la misma raíz o docker compose)
VITE_OPERATOR_BASE_URL=http://localhost:8011
# Alias legacy (se mantiene por compatibilidad)
VITE_API_BASE_URL=/

# WebSocket URL (por defecto ws://localhost:8011/ws)
VITE_WS_URL=ws://localhost:8011/ws
# Alias legacy
VITE_WS_BASE_URL=

# Token (no incluir secretos reales aquí; usar variables de entorno/CI)
VITE_VX11_TOKEN=vx11-local-token

# ============================================================
# Feature Flags
# ============================================================
# Habilita panel de logs en la UI
VITE_ENABLE_LOGS=true

# Habilita acceso a funciones administrativas
VITE_ENABLE_ADMIN=false
